---
import Navigation from "@src/components/Navigation.astro";
import type { Route } from "@src/types";

const home = { name: "feelsufo", url: "/" };
const routes: Route[] = [
  { name: "ABOUT", url: "/about/" },
  { name: "PROJECTS", url: "/projects/" },
];
---

<header>
  <a href={home.url}>
    <h1>Ken Maeshima</h1>
  </a>
  <Navigation routes={routes} />
</header>
