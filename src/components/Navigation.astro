---
import type { Route } from "@src/types";

type Props = {
  routes: Route[];
};

const { routes } = Astro.props;
---

<nav>
  <ul>
    {
      routes.map((route) => (
        <li>
          <a href={route.url}>{route.name}</a>
        </li>
      ))
    }
  </ul>
</nav>
